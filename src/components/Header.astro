---
import Navbar from '@components/Navbar.astro';

interface Props {
  navItems: {
    path: string;
    title: string;
  }[];
}

const {navItems} = Astro.props;
---

<header class="header">
        <Navbar {navItems}/>
</header>