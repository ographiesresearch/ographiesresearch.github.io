---
interface Props {
  columns?: boolean;
}

const {columns} = Astro.props;
---

<div class={columns ? "column is-half" : ""}>
    <div class="box has-background-danger box-shadow-white">
        <slot/>
    </div>
</div>