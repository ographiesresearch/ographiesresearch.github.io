---
import Header from '@components/Header.astro';
import Footer from '@components/Footer.astro';
import { SEO } from "astro-seo";
import { ClientRouter } from "astro:transitions";

import '../styles.scss';

interface Props {
  name: string;
}

const navItems = null;

import { type ConfigModel } from '@ts/Models'
import dataRaw from "@src/config.json";
const data:ConfigModel = dataRaw;


---
<!DOCTYPE html>
<html lang="en">
	<head>
		<SEO
			title=`${data.name} ${data.subtitle}`,
			description={data.description},
			charset="utf-8",
			canonical="https://ographiesresearch.com",
			openGraph={{
				basic: {
					title: `${data.name} ${data.subtitle}`,
					type: "website",
					image: "https://github.com/ographiesresearch/ographiesresearch.github.io/raw/refs/heads/main/src/assets/logo_medium_red.png",
					url: "https://ographiesresearch.com"
				},
				optional: {
					description: `${data.description}`,
    				locale: "en_US",
					siteName: `${data.name} ${data.subtitle}`
				}
			}}
		/>
    	<meta charset="utf-8">
		<link rel="sitemap" href="/sitemap-index.xml" />
		<link rel="icon" type="image/png" href="/favicon.png" />
		<meta name="viewport" content="width=device-width, initial-scale=1">
		<meta name="generator" content={Astro.generator} />
	</head>
	<body>
		<section class="section is-fullheight">
			<div class="container" id="content">
				{ navItems &&
					<Header {navItems}/>
				}
				<slot/>
			</div>
		</section>
		{ navItems && 
			<Footer navItems = {navItems}, parent = {data.parent}/>
		}
	</body>
</html>
<ClientRouter />